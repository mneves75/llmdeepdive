# 2026-08-04

- Continued the Signal Observatory `img2threejs` pipeline from image analysis
  through the bounded blockout review loop.
- Reused the production `TransformerScene` instead of maintaining a second
  generated model factory. The blockout now has eight separated decks, four
  posts, a rear spine, stepped plinth, post collars, deck sockets, a central
  signal rail, and three residual bypass routes.
- Calibrated the fixed review camera to azimuth `54°`, elevation `30°`. The
  final Tier 1 result was silhouette IoU `0.7163`, aspect delta `0.0470`, scale
  delta `0.0127`, and symmetry error `0.0777`. Left/rear orbit ratios were
  `0.8316` and `0.8140`, so the model is volumetric rather than a billboard.
- `orchestrate_passes.py` correctly rejected the blockout because IoU stayed
  below `0.85`. A single `refine-code` review action advanced the authority to
  its hard stop: `max-correction-loops-reached:blockout:3/3`. No downstream
  material, part-coverage, or action-ready gate was claimed.
