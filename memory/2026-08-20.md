# 2026-08-20

- Audited all 106 lessons in EN and pt-BR against primary sources and the
  published Qwen3.8-27B deployment campaign.
- Corrected technical and arithmetic defects across tracks 0–9, including the
  reference Gated DeltaNet state from 72 MiB to 144 MiB per sequence in float32.
- Integrated protocol-bound benchmark evidence for reasoning modes, realized
  context, correctness-first speculative decoding and harness failures.
- Redistributed every quiz answer key in paired locale positions and added a
  regression gate against degenerate distributions.
- Added parser-backed bilingual math/number-format checks and deterministic
  privacy helper tests.
- Full content gates, tests, typecheck, build, accessibility, bundle budget,
  privacy and dependency audit passed before final browser QA.
- Browser QA found the memory lab did not react to select change events; added
  a `change` listener and rechecked the production preview: the 8,192-token case now renders 656 MiB per sequence and 36 concurrent sequences.
- Independent review then corrected the pinned Q4_K_M size to 17.1 GB, the MI300X example to 208 8K or 8 full-context sequences, and removed hard throughput bounds inferred from checkpoint/file size.
