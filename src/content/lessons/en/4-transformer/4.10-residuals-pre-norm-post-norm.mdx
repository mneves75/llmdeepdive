---
id: "4.10-residuals-pre-norm-post-norm"
track: "4-transformer"
order: 10
title: "Residuals, pre-norm vs post-norm"
summary: "Qwen3.8-27B carries one 5120-dimensional residual stream through 64 heterogeneous layers, with pre-norm RMSNorm calibrating what each sublayer reads."
tier: "core"
locale: "en"
prerequisites: ["4.9-feed-forward-block"]
unlocks: ["4.11-full-transformer-block"]
analogy: "A long restoration project keeps the original photograph on a central light table. Sixty-four specialists — some retouchers, some archivists with different tools — each receive a calibrated copy, propose a bounded correction, and add it back rather than repainting from scratch. Pre-norm calibrates what enters each specialist while leaving the central table continuous. Post-norm adds the correction first and recalibrates the combined image afterward."
teachBack:
  prompt: "Write the conceptual equations for pre-norm and post-norm, explain how RMSNorm differs from LayerNorm, and say why one continuous residual stream lets Qwen3.8-27B stack 64 layers of two different kinds."
  modelAnswer: "Pre-norm computes x' = x + F(RMSNorm(x)); post-norm computes x' = Norm(x + F(x)). RMSNorm divides by the root-mean-square magnitude plus a small epsilon (1e-6 in Qwen3.8-27B) and applies a learned scale, without subtracting the mean as LayerNorm does. The additive identity path lets information and gradients travel through the stack without every layer reconstructing the representation, which is why 64 layers — whether their mixer is attention or Gated DeltaNet — can all read from and write to the same 5120-dimensional stream. Each sublayer only has to honor the interface: read a normalized copy, return an update of matching shape."
quiz:
  - question: "Which expression describes the pre-norm pattern Qwen3.8-27B uses?"
    options: ["x + F(RMSNorm(x))", "RMSNorm(x + F(x))", "F(x) with no residual path", "softmax(RMSNorm(x)) alone"]
    correctIndex: 0
    explanation: "Pre-norm normalizes the sublayer input and adds the resulting update to the untouched residual stream; post-norm would normalize the sum instead."
  - question: "How does RMSNorm differ from classic LayerNorm?"
    options: ["It divides by root-mean-square magnitude with a learned scale, without subtracting the feature mean", "It normalizes across the batch dimension", "It requires a bias term at every position", "It replaces the residual connection entirely"]
    correctIndex: 0
    explanation: "RMSNorm skips mean subtraction and rescales by RMS magnitude plus epsilon, a cheaper operation that works well in large decoder stacks."
  - question: "In the previous lesson's gated FFN, which tensor does the residual addition receive?"
    options: ["The down projection's output, back at width 5120, added to the stream that entered the sublayer", "The 17408-wide gated activations directly", "The SiLU gate values alone", "The normalized input, discarded after use"]
    correctIndex: 0
    explanation: "The FFN must project from 17,408 back to 5120 so its update matches the residual stream's shape; the addition then superposes it onto the existing state."
citations:
  - title: "Deep Residual Learning for Image Recognition"
    authors: "Kaiming He, Xiangyu Zhang, Shaoqing Ren, and Jian Sun"
    year: 2015
    url: "https://arxiv.org/abs/1512.03385"
  - title: "On Layer Normalization in the Transformer Architecture"
    authors: "Ruibin Xiong et al."
    year: 2020
    url: "https://arxiv.org/abs/2002.04745"
  - title: "Qwen3.8-27B Model Card"
    authors: "Qwen Team"
    year: 2026
    url: "https://huggingface.co/Qwen/Qwen3.8-27B"
updated: "2026-08-18"
---

Here is a problem the next lesson will make vivid: Qwen3.8-27B stacks 64 layers, and they are not even all the same kind of layer — most mix tokens one way, a minority another way, and every one of them ends in the wide gated FFN you just met. How do sixty-four heterogeneous processing stages cooperate without any stage destroying what earlier stages built, and without gradients dying on the way back down? The answer is the architecture's quietest and most load-bearing decision: a single continuous **residual stream**, 5120 numbers wide per token, that no layer ever replaces — layers only add to it.

For a transformation $F$, the core pattern is

$$
x' = x + F(x).
$$

The identity term $x$ gives information a direct route through the stack. A layer can learn a small useful correction instead of rebuilding the representation from scratch. During backpropagation, gradients also get an additive route that does not multiply through every sublayer's Jacobian. This does not abolish exploding or vanishing gradients, but it makes optimizing genuinely deep stacks tractable — and 64 layers is genuinely deep.

Each layer writes twice. After its mixing sublayer, the stream holds its previous features plus a communication update; after the FFN, it holds that result plus a nonlinear position-wise update. The residual stream is therefore not any single component's output. It is a shared workspace in which many overlapping features are superposed, and the fact that both a Gated DeltaNet layer and a full-attention layer can write to the same workspace — using the same interface, an added 5120-wide update — is exactly what lets Qwen mix layer types freely.

Normalization controls what each sublayer reads from that workspace. Classic LayerNorm computes a mean and variance across a token's feature dimension, subtracts, divides, and applies a learned scale and shift. Qwen3.8-27B, like most modern decoders, uses **RMSNorm**: divide the vector by its root-mean-square magnitude, add a small $\epsilon$ for numerical safety, and apply a learned scale — no mean subtraction, no shift. In Qwen the epsilon is $10^{-6}$: small enough to be invisible for healthy activations, present so a near-zero vector never divides by zero.

Where the normalization sits is the classic fork. The original Transformer used **post-norm**:

$$
x' = \operatorname{Norm}(x+F(x)),
$$

renormalizing the combined state after every addition. Walk the naive reasoning first, because it feels right: "normalize the final sum — surely the cleanest signal comes from calibrating the result." But trace the gradient. In post-norm, the identity path itself passes through the normalization, so every backward step multiplies through a norm's Jacobian; with 64 layers, that is 128 sublayer normalizations standing between the loss and the embedding. Deep post-norm stacks historically demanded careful warmup and were prone to divergence. The correction is **pre-norm**:

$$
x' = x+F(\operatorname{Norm}(x)),
$$

which is what Qwen3.8-27B does. Each sublayer receives a calibrated copy, but the main path remains a bare chain of additions — the light table never gets repainted. Concretely, one Qwen layer computes $x_1 = x + \operatorname{Mixer}(\operatorname{RMSNorm}(x))$ and then $x_2 = x_1 + \operatorname{FFN}(\operatorname{RMSNorm}(x_1))$, and the model repeats that contract 64 times before a final RMSNorm prepares the state for the output head.

Pre-norm has its own dynamics. Because the stream is never rescaled on the main path, its magnitude tends to grow as updates accumulate through depth; the per-sublayer RMSNorm hides that growth from each reader, and the final norm hides it from the head. Initialization, learning-rate schedule, precision, and optimizer all interact with these choices — pre-norm is an empirically friendlier default for very deep stacks, not a theorem about accuracy.

:::caveat
"Pre-norm" is not a complete specification, and neither is "RMSNorm." A model may normalize its two sublayers differently, add sandwich or QK-norms, scale residual branches, or place extra norms around embeddings and output. The most common inspection mistake is assuming a checkpoint's norm parameters mean the same thing as in your reference implementation; read the actual block equations before assuming compatibility.
:::

The phrase "residual stream" also disciplines interpretation. Because features are superposed in one vector space, a mixing layer's output may be cancelled by a later update, carried unchanged for thirty layers, or transformed downstream. Looking at one sublayer's output in isolation misses both the state already present in $x$ and the later operations that consume the sum.

A practical debugging habit: record three norms per sublayer — the stream $\lVert x\rVert$, the update $\lVert F(\cdot)\rVert$, and the sum. An update that dwarfs the stream can destabilize training; one that is always negligible may be a dead branch. These are diagnostics, not thresholds; expected scale depends on depth and architecture.

The enduring picture is collaborative editing at industrial depth. Qwen3.8-27B's stream preserves the current document across 64 specialists of two different trades. RMSNorm hands each specialist a calibrated copy at a cost of one scale vector per sublayer, epsilon $10^{-6}$ guarding the division. Pre-norm placement keeps the master document continuous, which is precisely why the stack can be this deep — and this heterogeneous — without collapsing.
