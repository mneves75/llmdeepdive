---
id: "4.17-multimodal-vision-encoder"
track: "4-transformer"
order: 17
title: "How images join the sequence: the vision encoder"
summary: "Qwen3.8-27B turns images and video into sequence elements through a 27-layer vision tower — 16×16 pixel patches embedded at width 1152, encoded, and projected into the same 5120-dimensional stream as text, positioned by three-part MRoPE."
tier: "core"
locale: "en"
prerequisites: ["1.5-embeddings-meaning-as-geometry", "4.11-full-transformer-block"]
unlocks: []
analogy: "Text tokens get their vectors from a dictionary: look up the row, done. Images pass through a darkroom instead: each 16-by-16 tile of the photograph is developed — through twenty-seven stages of comparison with every other tile — into a vector that no dictionary contains, then printed at the same size as the dictionary's entries so both can lie on one conveyor. Each developed tile is stamped with three counters — which frame, which row, which column — so the reader downstream knows not just that a tile arrived, but where and when it belongs."
teachBack:
  prompt: "Trace an image through Qwen3.8-27B's vision tower into the decoder's sequence, and explain how MRoPE's three sections position visual and textual elements."
  modelAnswer: "The image is cut into 16×16 pixel patches, each linearly embedded into the vision tower's 1152-dimensional space. Twenty-seven encoder layers with 16 attention heads and FFN width 4304 let every patch exchange information with every other patch. A projection then maps the resulting vectors to 5120 dimensions so they enter the decoder's residual stream alongside text embeddings — as continuous vectors, not vocabulary entries. The decoder's rotary position embedding is split into three sections of 11, 11, and 10 frequency pairs encoding temporal, height, and width indices: image patches vary in height and width, video frames advance the temporal index, and text advances all coordinates together, recovering ordinary 1D RoPE behavior."
quiz:
  - question: "In lesson 1.5, a text token's vector is a row of the 248,320×5120 embedding table. Where does an image patch's 5120-dimensional vector come from?"
    options: ["It is computed — patch pixels embedded at 1152, transformed by 27 encoder layers, then projected to 5120 — and corresponds to no row of any table", "From a special image section of the vocabulary table", "From the nearest text token's row", "From the lm_head, run in reverse"]
    correctIndex: 0
    explanation: "Visual inputs bypass the vocabulary entirely: their vectors are produced by the vision tower and projector, so the space of possible image inputs is continuous rather than a finite token inventory."
  - question: "What do MRoPE's three sections of 11, 11, and 10 frequency pairs encode?"
    options: ["Temporal, height, and width positions, so a patch's location in frame, row, and column is expressed in the rotary embedding", "Red, green, and blue channels", "Past, present, and future tokens", "Three independent vocabularies"]
    correctIndex: 0
    explanation: "The decoder's rotary dimensions are partitioned so position is three-coordinate: video advances the temporal section, spatial layout drives height and width, and text advances all three in step."
  - question: "Why must the vision tower's output pass through a projection before entering the decoder?"
    options: ["The tower works at width 1152 while the decoder's residual stream is 5120 wide, and residual addition demands matching shape", "To convert the patches into vocabulary IDs", "To apply the causal mask to the image", "To compress the image below one kilobyte"]
    correctIndex: 0
    explanation: "As lesson 4.11 established, everything joining the stream must arrive at model width; the projector is the adapter between the tower's 1152 and the decoder's 5120."
citations:
  - title: "Qwen3.8-27B Model Card"
    authors: "Qwen Team"
    year: 2026
    url: "https://huggingface.co/Qwen/Qwen3.8-27B"
  - title: "An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale"
    authors: "Alexey Dosovitskiy et al."
    year: 2020
    url: "https://arxiv.org/abs/2010.11929"
updated: "2026-08-18"
---

The decoder you assembled in this track consumes exactly one thing: a sequence of 5120-dimensional vectors. Text has an obvious route in — tokenize, look up rows of the embedding table, done. But Qwen3.8-27B also accepts images and video, and a photograph has no tokens: it is a grid of pixel intensities with no vocabulary, no discrete units, no lookup table. The concrete problem of multimodality is therefore an adapter problem: manufacture, from raw pixels, vectors that can stand in the same sequence as word embeddings and be processed by the same 64 layers. The component that does this is the **vision encoder**, and in Qwen3.8-27B it is a 27-layer Transformer of its own.

The recipe is the Vision Transformer's, from Dosovitskiy and colleagues: if Transformers eat sequences, cut the image into a sequence. The image is divided into square patches of **16×16 pixels**, and each patch — a small tile of raw intensities — is flattened and linearly embedded into the tower's working width of **1152**. A patch plays the role a token plays in text: the atomic sequence element. A modest photograph yields a few hundred patches; as a worked instance, a 224×224 crop cuts into a 14×14 grid, 196 patches, each now a 1152-dimensional vector.

Those raw patch embeddings are nearly useless alone — a tile of blue pixels could be sky, sea, or a car door. Meaning comes from context, and the tower supplies it the way you would now expect: **27 Transformer layers**, each with **16 attention heads** and an FFN of intermediate width **4304**, in which every patch attends to every other patch. No causal mask applies — an image has no "future" to hide, so the tower runs encoder-style, bidirectionally, exactly the connectivity lesson 4.12 assigned to models that see their whole input at once. Twenty-seven rounds of communicate-then-compute later, each patch's vector encodes not just its own pixels but its role in the scene.

One mismatch remains: the tower speaks 1152, the decoder's residual stream speaks 5120. A learned projection maps each encoded patch vector to **5120 dimensions**, and with that, the adapter is complete. The projected patch vectors are spliced into the token sequence alongside ordinary text embeddings, and from the decoder's point of view they are simply more sequence elements: attended to by the 16 attention layers, folded into state by the 48 DeltaNet layers, transformed by every gated FFN. The model does not have a "vision mode" — it has a sequence in which some positions happened to be manufactured by the tower instead of looked up in the table.

Here is where a natural wrong model creeps in, so run the check explicitly. The tempting story: "the vision encoder converts the image into tokens — it captions the picture internally, and the decoder reads those words." If that were true, each patch vector would need to match a row of the 248,320-entry embedding table, and the tower's expressible outputs would be capped at the vocabulary. Check against the mechanism: the projector emits arbitrary points in the continuous 5120-dimensional space, subject to no lookup, no nearest-row snapping, no discretization. A patch vector almost never coincides with any word's row. The image is not translated into language before understanding; it enters the same geometric space lesson 1.5 introduced — where meaning is position, not spelling — through a different door.

:::caveat
"Image tokens" is a convenient phrase and a misleading ontology. Text tokens are drawn from a finite inventory and own rows of the embedding table; visual sequence elements are continuous vectors computed fresh from pixels, corresponding to no vocabulary entry. Conflating the two leads to real errors — such as expecting image content to be reachable by token IDs, or counting the vocabulary as if it bounded what the model can perceive.
:::

One question remains: position. Text positions are one-dimensional — token 7 follows token 6 — and lesson 4.7 showed how RoPE rotates query–key pairs to encode that order. But a patch's position is inherently spatial (row 3, column 11), and a video patch adds time (frame 40). Qwen3.8-27B answers with **MRoPE**, multimodal rotary position embedding: the decoder's rotary frequency pairs are partitioned into three interleaved sections of **11, 11, and 10 pairs**, dedicated respectively to **temporal, height, and width** coordinates. Every sequence element carries a three-part position. For an image patch, the height and width sections express the patch's grid coordinates while the temporal index holds still; for video, the temporal section advances frame by frame, letting attention measure "two frames earlier" as naturally as "three tokens back"; for plain text, all three indices advance together, and MRoPE degenerates gracefully into the ordinary 1D RoPE the text-only lessons assumed. Position, like meaning, turns out to be extensible: not abandoned for multimodality, but given more axes.

Step back and the architecture completes. The vision tower is part of the ~27B parameter total, a specialist encoder bolted to a hybrid decoder through a single projection. Its patches ride the same residual stream, are retrieved by the same attention layers, and obey the same block contract from lesson 4.11 — arrive at width 5120 or do not arrive at all. Understanding it required nothing new: patchify is tokenization by scissors, the tower is the encoder family you already classified, the projector is a linear map, and MRoPE is RoPE with three counters instead of one. Multimodality, in this model, is not a second brain. It is a second door into the same one.
