---
id: "1.1-why-computers-cant-read"
track: "1-text-to-tensors"
order: 1
title: "Why computers can't read"
summary: "Text reaches Qwen3.8-27B as bytes, becomes token ids drawn from 248,320 possibilities, and comes back out again; every stage has a precise contract and none of them supplies meaning."
tier: "foundations"
locale: "en"
prerequisites: ["0.8-how-to-read-ml-paper"]
unlocks: ["1.2-tokenization-i"]
analogy: "A stage technician receives a crate of numbered tiles instead of a play. The numbers preserve every letter and mark faithfully, and the crate can be emptied back into the original script without losing a comma. What the numbers do not identify is a joke, a promise, or who a pronoun refers to. An encoding is the inventory sheet mapping numbers to marks. A tokenizer decides how many marks travel per tile. Meaning is printed on no tile at all."
teachBack:
  prompt: "Trace one string from a keyboard into Qwen3.8-27B and back out, naming what each layer guarantees, and explain why the round trip proves nothing about understanding."
  modelAnswer: "The characters are encoded, normally as Unicode code points serialized to UTF-8 bytes. The tokenizer groups those bytes into vocabulary units and emits integer ids, each one an index into an embedding table with 248320 rows. The embedding lookup returns a 5120-dimensional vector per token, which the 64 layers repeatedly rewrite. The final layer produces a score for every one of the 248320 vocabulary entries, a token is selected, and the tokenizer's decoder turns the chosen ids back into bytes and then into displayable text. Each layer guarantees exactly one thing: bytes preserve data, encodings map bytes to symbols, tokenizers map symbols to ids, embeddings map ids to vectors. Lossless round-tripping is a property of the encoder and decoder, not evidence that anything in between comprehended the sentence."
quiz:
  - question: "The word café can be typed as four code points ending in a precomposed accented e, or as five code points ending in e followed by a combining accent. What does Qwen3.8-27B see?"
    options: ["Different byte sequences, so potentially different token ids, even though both render identically", "The same tokens, because the tokenizer normalizes all text", "An error, because only one form is valid Unicode", "The same tokens, because the two forms have the same number of characters"]
    correctIndex: 0
    explanation: "In UTF-8 the first form is five bytes and the second is six. The model receives whatever the tokenizer produced from those bytes; unless normalization was applied first, two visually identical strings can enter as different sequences."
  - question: "Why is token id 500 not inherently more meaningful than token id 20?"
    options: ["Ids are arbitrary indices into the embedding table, and only the learned row at that index matters", "Higher ids are reserved for punctuation", "Only even ids receive embeddings", "Ids are sorted by frequency, so lower is always more common"]
    correctIndex: 0
    explanation: "Feeding an id as a magnitude would falsely imply that token 501 is nearer to 500 than token 20 is. The embedding lookup exists precisely to break that false ordering."
  - question: "Lesson 0.8 distinguished config-verified facts from vendor claims. Which of these about Qwen3.8-27B is config-verified?"
    options: ["That its embedding table has 248,320 rows", "That it reasons about scientific questions well", "That it was trained on a particular mixture of web text", "That it is the best open model for its size"]
    correctIndex: 0
    explanation: "The vocabulary size is declared in the configuration and must match the shipped tensors. The other three are respectively a vendor benchmark claim, unpublished, and a comparison nobody ran in front of you."
citations:
  - title: "The Unicode Standard, Version 17.0"
    authors: "The Unicode Consortium"
    year: 2025
    url: "https://www.unicode.org/versions/Unicode17.0.0/"
  - title: "Qwen3.8-27B Model Card"
    authors: "Qwen Team"
    year: 2026
    url: "https://huggingface.co/Qwen/Qwen3.8-27B"
updated: "2026-08-18"
---

Type the word *café* into a chat box and send it to Qwen3.8-27B. A perfectly ordinary thing to do, and it hides a problem that has broken production systems. There are two standard ways to represent that word in Unicode. One uses four code points, ending in a single precomposed character for the accented e. The other uses five, ending in a plain e followed by a separate combining acute accent. On screen they are indistinguishable. Serialized to UTF-8 the first occupies five bytes and the second occupies six, because the accented character costs two bytes in the first form and the bare e plus combining mark costs three in the second.

The model never sees your screen. It sees the bytes. Two strings that a human would swear are the same word can therefore enter the network as different token sequences, produce different hidden states, and yield different answers. Nothing has gone wrong; every layer did exactly its job. What went wrong is an assumption that the word was the unit. It never was.

So follow the whole path, naming what each stage promises and what it does not. The first boundary is **encoding**. Unicode assigns code points to a vast repertoire of text elements, and UTF-8 defines how each code point becomes one to four bytes. A byte value on its own is meaningless without knowing the encoding that produced it. Even the notion of a visible character is slippery: an emoji with a skin-tone modifier and a zero-width joiner can occupy several code points while drawing as one glyph, and the *café* example shows that even a Latin word with one accent has more than one legal spelling in bytes.

Normalization can force canonically equivalent forms into agreement, but applying it is a policy decision rather than a default truth. Identifiers, passwords, and source code often must preserve distinctions that natural-language text is happy to lose. Whitespace carries the same hazard: an ordinary space, a non-breaking space, and a narrow no-break space look nearly identical and are entirely different bytes. Every one of these choices is made before any model is involved.

The second boundary is **tokenization**. A tokenizer maps a byte sequence to a sequence of vocabulary entries and their integer ids. For Qwen3.8-27B those ids index a table with 248,320 rows, and the ids themselves are labels, not quantities. Token 900 is not larger, later, or more important than token 12. The tokenizer for this model is byte-level, which has one enormously useful consequence: because every possible byte value is representable, no input can be rejected as unknown. Corrupted data, an unfamiliar script, or a raw binary blob all encode to *something*. Lesson 1.2 examines what that vocabulary contains and lesson 1.3 shows how it was built.

Run the round trip in your head. The five or six bytes of *café* become some short list of ids. Each id selects a row of the embedding table, producing a 5120-dimensional vector as lesson 0.4 described. Those vectors pass through 64 layers, each rewriting the state. The final layer emits 248,320 scores for the next position, a token is chosen, and the decoder maps chosen ids back to bytes and then to text you can read. Encode and decode are inverses: a well-behaved byte-level tokenizer returns exactly the bytes you gave it, punctuation, spacing, and all.

:::caveat
This lesson deliberately does not print the specific integer ids that Qwen assigns to *café*, and you should be suspicious of any course that does from memory. Lesson 0.8 made the rule: architectural facts are stated only when a primary artifact declares them. The vocabulary size is in the configuration file and is safe to quote. Individual token ids are a property of the tokenizer files, which you can load and query in seconds, and which no one should paraphrase from recollection.
:::

An embedding table maps ids to vectors, and at initialization those vectors contain no definitions. Training reshapes them, along with everything downstream, so that the representations become useful for prediction. Notice that the *same* row serves every occurrence of a token. The word *bat* in a cave and *bat* in baseball begin at one identical row; it is the surrounding tokens, mixed in by the layers above, that pull the two states apart. Meaning, insofar as the word applies at all, is a property of a contextual state rather than of a vocabulary entry.

Syntax and semantics are therefore delivered by neither Unicode nor the tokenizer. They emerge, imperfectly, from patterns in the training data, from architectural bias, and from the training objective. A model can learn that an opening quotation mark predicts a later closing one, that a subject constrains a verb, or that certain names co-occur with certain professions. Those regularities can support genuinely useful behavior without amounting to an explicit grammar or a checked knowledge base.

:::warning
The misconception worth naming is that a lossless round trip demonstrates comprehension. It demonstrates that the encoder and decoder are inverses, which is a property of about two hundred lines of tokenizer code and would hold just as perfectly if the 64 layers in between were replaced with random numbers. Fidelity of transport tells you nothing about the quality of what happened in transit.
:::

The layered view explains a catalogue of real failures. Corrupted bytes break decoding before the model is reached. Visually confusable characters can hide a substituted identifier in what looks like ordinary text. An unlucky tokenizer can shatter one language into many more positions than another, which lesson 1.4 turns into money. Rare spellings receive weakly trained representations. And a fluent model can still attach the wrong real-world referent, because its objective rewarded probable continuation rather than grounded verification.

Computers process text with extraordinary sophistication, and every stage of that processing has a contract narrow enough to write on one line. Bytes preserve data. Encodings map bytes to symbols. Tokenizers map symbols to ids. Embeddings map ids to vectors. Layers transform vectors. Decoding maps scores back to tokens. Keeping those six boundaries distinct is what stops the vague verb *reading* from concealing where information entered, changed, or was quietly lost.
