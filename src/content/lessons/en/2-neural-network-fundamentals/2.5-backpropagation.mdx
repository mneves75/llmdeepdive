---
id: "2.5-backpropagation"
track: "2-neural-network-fundamentals"
order: 5
title: "Backpropagation, visualized"
summary: "Reverse-mode differentiation gets every parameter's gradient in one backward sweep — the only reason a 27-billion-parameter model can be trained at all."
tier: "foundations"
locale: "en"
prerequisites: ["2.4-loss-functions-cross-entropy"]
unlocks: ["2.6-gradient-descent-loss-landscape"]
analogy: "A bakery records every transformation from flour to finished loaves. When the final loaf is too dense, a reviewer walks the ledger backward. At each station, they combine the complaint arriving from downstream with that station's local sensitivity: how kneading affected rise, how water affected dough, and how flour affected water absorption. Shared ingredients receive complaints from every path. The backward audit assigns responsibility efficiently without baking one new loaf per knob."
teachBack:
  prompt: "Explain forward pass, computational graph, upstream gradient, local derivative, and accumulation — then say why reverse mode rather than finite differences is what makes training a 27B-parameter model feasible."
  modelAnswer: "The forward pass computes intermediates and a scalar loss, recording a graph of operations. Reverse mode starts from dL/dL=1, and each node multiplies the incoming upstream gradient by its own local derivative before passing contributions to its parents; when a value feeds several consumers, the contributions add. The cost of the whole backward sweep is a small constant multiple of one forward pass, independent of parameter count. Estimating the same gradients by finite differences would need two forward passes per parameter — tens of billions of them for a model such as Qwen3.8-27B — which is why reverse mode is not an optimization but a precondition."
quiz:
  - question: "For softmax with cross-entropy from lesson 2.4, what is the gradient of the loss with respect to the logits?"
    options: ["The predicted distribution minus the one-hot target, p − y", "The negative log of the target probability", "Always zero for the correct class", "The softmax of the gradient"]
    correctIndex: 0
    explanation: "This unusually clean form is why libraries fuse softmax and cross-entropy: the correct class gets a negative push, every other class a positive one proportional to the probability it wrongly claimed."
  - question: "Why are gradient contributions added at a branch point?"
    options: ["The value affects the loss through multiple downstream paths, and the multivariable chain rule sums over all of them", "Addition replaces the chain rule", "Only one path has a derivative", "The optimizer requires integers"]
    correctIndex: 0
    explanation: "This is also why autodiff frameworks accumulate into existing gradient buffers rather than overwriting them, and why training loops must clear them at batch boundaries."
  - question: "What does backpropagation itself change?"
    options: ["Nothing — it computes gradients, and a separate optimizer changes parameters", "It tokenizes the batch", "It selects the labels", "It guarantees lower test loss"]
    correctIndex: 0
    explanation: "Differentiation and parameter update are distinct phases; conflating them hides which of the two is misbehaving."
citations:
  - title: "Learning representations by back-propagating errors"
    authors: "David E. Rumelhart, Geoffrey E. Hinton, and Ronald J. Williams"
    year: 1986
    url: "https://www.nature.com/articles/323533a0"
  - title: "Qwen3.8-27B Model Card"
    authors: "Qwen Team"
    year: 2026
    url: "https://huggingface.co/Qwen/Qwen3.8-27B"
updated: "2026-08-18"
---

Lesson 2.4 produced a single number: the loss. To improve the model you need to know, for every parameter, whether nudging it up or down would make that number smaller. There is an obvious way to find out. Perturb one parameter by a tiny amount, run the model again, see how the loss moved, and divide. Repeat for the next parameter.

Price that plan honestly, because it is the wrong turn worth walking into. A careful two-sided estimate needs two forward passes per parameter. For a small network with ten thousand parameters that is twenty thousand forward passes per gradient — slow but survivable. For Qwen3.8-27B, with roughly 27 billion parameters (Qwen3.8-27B Model Card, 2026), it is 54 billion forward passes to obtain the gradient for a *single* training step. If one forward pass took a millisecond, one step would take about twenty months. Finite differences do not scale badly; they do not scale at all.

**Backpropagation** is what replaces it, and the improvement is not incremental. One backward sweep produces the gradient for every parameter at once, at a cost of roughly the same order as one forward pass, no matter how many parameters there are.

The mechanism is the chain rule applied to a recorded graph. The **forward pass** evaluates operations in order, saving the intermediate values that derivatives will need. The **backward pass** starts at the end with $\partial L/\partial L=1$ and walks the operations in reverse, and at every operation the rule is the same: **upstream gradient times local derivative**. Each operation only needs to know how its own outputs respond to its own inputs; it never needs to know anything about the rest of the network.

Do one by hand, using the loss from lesson 2.4. Take three logits $z=(2.0,\,1.0,\,0.1)$ with the observed class being the first. Forward: exponentials are about $7.389$, $2.718$, and $1.105$, summing to $11.212$, so $p\approx(0.659,\,0.242,\,0.099)$ and $L=-\ln 0.659\approx0.417$. Backward: for softmax followed by cross-entropy the gradient with respect to the logits collapses to the beautifully simple $\partial L/\partial z=p-y$, which here is $(0.659-1,\;0.242-0,\;0.099-0)=(-0.341,\,0.242,\,0.099)$. Read it as an instruction. The correct class carries a negative gradient, so gradient descent will raise its logit. The two wrong classes carry positive gradients in exact proportion to the probability they took, so their logits get pushed down, the more confidently wrong one hardest. Now suppose those logits came from $z=Wh+b$ for a hidden vector $h$. The same rule continues: $\partial L/\partial b=p-y$, $\partial L/\partial W=(p-y)h^{\top}$, and $\partial L/\partial h=W^{\top}(p-y)$ — that last one is the message handed to the layer below, which will apply the identical procedure with its own local derivative.

Graphs branch, and branching is where implementations get subtle. If a value influences the loss along two paths, its gradient is the **sum** of the two contributions, because that is what the multivariable chain rule says. This is why autodiff frameworks accumulate into gradient buffers rather than overwriting them, and consequently why a training loop must zero those buffers at batch boundaries: summation is the mathematically correct default for repeated use, and only the loop knows where one batch ends.

:::insight
Now scale the picture to this course's model. Qwen3.8-27B stacks 64 layers, each carrying a hidden state 5120 wide per token (Qwen3.8-27B Model Card, 2026). A backward pass is a single sweep from the loss at the top down through all 64 layers, and at each one it produces both the gradients for that layer's matrices and the message passed further down. The remarkable fact is not that this is fast. It is that the cost does not grow with the number of parameters being differentiated — one sweep, every gradient, which is the only reason models of this size exist.
:::

The bill arrives as memory rather than time. The backward pass needs values the forward pass computed, so training holds activations alive from the moment they are produced until their gradients are consumed. Multiply that by 64 layers, by the intermediate width of 17408 inside each feed-forward block, by every token position in the batch, and activation memory rivals or exceeds parameter memory. **Gradient checkpointing** buys memory back by storing only selected boundaries and recomputing the rest during backward — more compute for less memory, and a lever lesson 5.10 turns explicitly.

:::warning
The most common misconception is that a small or zero gradient signals a bug in differentiation. It usually does not. Backpropagation faithfully reports the derivative of the computation it was handed, so an uninformative gradient is normally a fact about the model's geometry — a saturated activation, a dead unit, products of many small local derivatives across depth — and the fix belongs to architecture, initialization, or normalization rather than to autodiff. What *is* a bug is a severed path: converting a tensor to a plain number, mutating a saved value in place, or running a region under a no-gradient context. Those produce a gradient of exactly zero or none at all, which is a different symptom from a small one.
:::

Two habits keep this honest. For any new custom operation, gradient-check it against finite differences on a tiny input — the method that fails at scale is perfectly good as a unit test, given sensible precision and step size, away from non-differentiable points. And before a long run, confirm that every parameter you expect to train has a finite, nonzero gradient after one loss. The enduring picture is a reverse flow of responsibility through a recorded graph, where purely local rules compose into global sensitivity, and where the optimizer of lesson 2.7 has not yet done anything at all.
