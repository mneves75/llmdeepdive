---
id: "8.6-tensorrt-llm"
track: "8-building-and-serving-stacks"
order: 6
title: "TensorRT-LLM"
summary: "Current TensorRT-LLM uses only its PyTorch backend: Hugging Face checkpoints load directly while kernels, scheduling, caching, and parallelism remain NVIDIA-specialized."
tier: "frontier"
locale: "en"
prerequisites: ["8.4-vllm", "7.9-quantization-i-int8-int4-the-basics"]
unlocks: ["8.12-choosing-your-stack"]
analogy: "A specialist workshop accepts the same standard blueprint as its competitors but owns NVIDIA-specific machines, jigs, and scheduling. You no longer cast a separate engine before opening the doors; you hand the current runtime a Hugging Face checkpoint. The specialization can still pay in throughput, but changing the model, precision, runtime, or traffic mix means revalidating the whole production line."
teachBack:
  prompt: "Explain the removal of the TensorRT backend, how a Hugging Face checkpoint is served today, and what must still be validated."
  modelAnswer: "The serialized TensorRT-engine backend was removed in August 2026. PyTorch is now the sole backend, with AutoDeploy on top. There is no conversion or trtllm-build step: trtllm-serve loads a Hugging Face checkpoint directly. NVIDIA kernels, in-flight batching, paged KV, quantization, speculative decoding, and parallelism remain. Revision, runtime, precision, memory, context, and Gated DeltaNet operator coverage still require validation."
quiz:
  - question: "From lesson 7.9, why is FP8 easier to apply to activations than INT8 despite both using eight bits?"
    options: ["FP8 keeps an exponent field, so it covers a wide dynamic range with coarse per-tensor scaling, while INT8 is uniform and needs tighter, more careful calibration to avoid clipping outliers","FP8 uses more bits than INT8","INT8 cannot represent negative numbers","FP8 is lossless"]
    correctIndex: 0
    explanation: "Activation outliers are what break naive integer quantization; a floating-point 8-bit format absorbs them in the exponent instead of the scale factor."
  - question: "A batch-1 test with 2K prompts beats vLLM. Why might it still be the wrong choice?"
    options: ["The runtime expires after a week","The trace does not represent production lengths or concurrency, so it measures another workload","It must produce different tokens from the reference","Batch 1 is always slower"]
    correctIndex: 1
    explanation: "Runtime and trace must reproduce the real workload; a number from another distribution is another experiment."
  - question: "What is in-flight batching?"
    options: ["Batching requests before they reach the server","Running prefill and decode on separate GPUs","TensorRT-LLM's iteration-level scheduling, admitting and retiring requests between decode steps rather than holding a fixed batch — the same idea as continuous batching in lesson 7.8","Capturing CUDA graphs during flight"]
    correctIndex: 2
    explanation: "The name differs, the mechanism does not: batch membership changes at iteration boundaries so a finished sequence frees its slot immediately."
citations:
  - title: "TensorRT-LLM Documentation"
    authors: "NVIDIA"
    year: 2026
    url: "https://nvidia.github.io/TensorRT-LLM/"
  - title: "TensorRT-LLM backend removal"
    authors: "NVIDIA"
    year: 2026
    url: "https://nvidia.github.io/TensorRT-LLM/latest/legacy/tensorrt-backend-removal.html"
  - title: "NVIDIA Data Center Technologies"
    authors: "NVIDIA"
    year: 2026
    url: "https://www.nvidia.com/en-us/data-center/"
  - title: "CUDA Toolkit Documentation"
    authors: "NVIDIA"
    year: 2026
    url: "https://docs.nvidia.com/cuda/"
  - title: "Qwen3.8-27B Model Card"
    authors: "Qwen Team"
    year: 2026
    url: "https://huggingface.co/Qwen/Qwen3.8-27B"
updated: "2026-08-20"
---

Your vLLM deployment works, and you want another NVIDIA-focused stack. Current documentation changes the premise: in August 2026 TensorRT-LLM removed its serialized TensorRT-engine backend. PyTorch is now the sole execution backend, with AutoDeploy remaining available on top of it.

There is no separate conversion or trtllm-build step. The current command is trtllm-serve pointed directly at a Hugging Face checkpoint. This is not eager PyTorch: the runtime still supplies NVIDIA-specialized kernels, in-flight batching, paged KV, quantization, speculative decoding, and parallelism. Pin the checkpoint and runtime, configure memory and scheduling, and benchmark the real workload; do not maintain an engine matrix by frozen shape.

Quantization can still release substantial serving capacity by shrinking the resident weights and, when the runtime supports a validated low-precision KV format, the cache as well. Do not infer a multiplier from bit width alone: lesson 7.2 owns the model-specific KV derivation, and lesson 9.3 owns the full accelerator budget, including runtime reserve and recurrent state. Recompute there with the exact artifact and runtime configuration, then require the correctness canary from lesson 8.1 before treating the recovered capacity as usable.

Serving is otherwise familiar. In-flight batching is TensorRT-LLM's name for the iteration-level scheduling of lesson 7.8: requests join and leave between decode steps rather than waiting for a fixed batch to drain. Paged KV, chunked prefill, and speculative decoding all have counterparts here. The server and configuration surface differ from vLLM, but the concepts transfer intact, which is why this lesson can spend its space on the parts that do not.

:::warning
A batch-1 benchmark with short prompts and outputs says nothing about 32K prompts, multimodality, or production concurrency. Replay the real input and output distributions, arrival pattern, and decoding parameters. Compare correct responses inside the SLO, not an isolated peak.
:::

The operational bill is now a compatibility and configuration matrix, not a serialized-engine matrix. A runtime update can change kernels, memory use, or operator coverage; a new precision can change quality; a new traffic profile can need different scheduling. Pin versions, preserve correctness canaries, record realized KV capacity, and revalidate after every material change.

:::caveat
Architecture support decides everything above, and for this model it must be verified per version rather than assumed. Qwen3.8-27B's 48 Gated DeltaNet layers need explicit implementation: kernels for the delta-rule update and the short convolution, and a runtime that manages a per-sequence recurrent state alongside the paged KV of the 16 attention layers. Compiled stacks typically gain support for a new architecture later than eager and Python-engine stacks do, because a plugin or kernel must be written rather than a module. Before planning any migration, confirm the exact release builds and runs this model, then run the differential test from lesson 8.1 against eager PyTorch. A server that starts cleanly and mishandles the recurrent state will be fast, confident, and wrong.
:::

Current TensorRT-LLM provides an NVIDIA-specialized runtime without a separate AOT engine. The price is NVIDIA ecosystem dependence and validation per version, precision, model, and workload. The right choice passes the same canaries and delivers more goodput inside the SLO.
