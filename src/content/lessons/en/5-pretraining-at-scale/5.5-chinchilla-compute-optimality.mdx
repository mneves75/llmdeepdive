---
id: "5.5-chinchilla-compute-optimality"
track: "5-pretraining-at-scale"
order: 5
title: "Scaling laws II: Chinchilla & compute-optimality"
summary: "Chinchilla's iso-compute experiments rebalanced parameters against tokens for a fixed training budget — and modern releases like Qwen3.8-27B deliberately sit far past that point, for reasons that have nothing to do with training."
tier: "advanced"
locale: "en"
prerequisites: ["5.4-kaplan-scaling-laws"]
unlocks: ["5.6-inference-aware-scaling"]
analogy: "A farm has a fixed fuel budget for one tractor season. Buying a huge tractor leaves little fuel to cross the field; buying a tiny one permits many passes but limits work per pass. Earlier measurements favored a larger tractor. Chinchilla-style iso-compute trials tested several tractor sizes at the same total fuel and found a more balanced pairing of machine and distance."
teachBack:
  prompt: "Explain the iso-compute method, compute where a 27-billion-parameter model would sit on a Chinchilla-style curve, and say why a shipped 27B model is unlikely to sit there."
  modelAnswer: "Iso-compute experiments train several model sizes under the same FLOP budget, varying token count inversely, and take the lowest-loss point at each budget. Hoffmann et al. fitted those optima and found parameters and tokens should grow at roughly equal rates, popularly summarized as about twenty tokens per parameter. Applying that to 27 billion parameters gives roughly 540 billion tokens and, at the 6ND estimate, about 8.7e22 FLOPs of training compute. A shipped model is unlikely to sit there because the Chinchilla optimum minimizes loss for a fixed training budget only; once serving cost enters, extra training tokens buy a permanently smaller model, so vendors over-train deliberately. For Qwen3.8-27B specifically no token count is published, so its actual position on the curve is unknown."
quiz:
  - question: "At a Chinchilla-style twenty tokens per parameter, roughly how many training tokens would a 27-billion-parameter dense model use, and what is the 6ND compute estimate?"
    options: ["About 540 billion tokens and about 8.7e22 FLOPs", "About 27 billion tokens and about 4.4e21 FLOPs", "About 5.4 trillion tokens and about 8.7e20 FLOPs", "It is published in the Qwen3.8-27B technical report"]
    correctIndex: 0
    explanation: "20 x 27e9 = 5.4e11 tokens; 6 x 27e9 x 5.4e11 = 8.748e22 FLOPs. There is no technical report for Qwen3.8-27B, and its real token count is not public."
  - question: "Lesson 5.4 warned about extrapolating a fitted power law. What is the equivalent error with the twenty-tokens-per-parameter figure?"
    options: ["Treating a ratio fitted at 2022 scale, corpus, and architecture as a constant that determines any later model's token count", "Assuming loss has an irreducible floor", "Plotting on log-log axes", "Holding compute fixed across a curve"]
    correctIndex: 0
    explanation: "It is a fitted optimum for one regime and one objective, not a law; both the fit and the objective it optimizes have moved since."
  - question: "What is held approximately fixed on an iso-compute curve?"
    options: ["Total training computation", "Parameter count only", "Dataset text only", "Inference latency only"]
    correctIndex: 0
    explanation: "Different parameter/token combinations are compared under a common training-compute budget."
citations:
  - title: "Training Compute-Optimal Large Language Models"
    authors: "Jordan Hoffmann et al."
    year: 2022
    url: "https://arxiv.org/abs/2203.15556"
  - title: "Qwen3.8-27B Model Card"
    authors: "Qwen Team"
    year: 2026
    url: "https://huggingface.co/Qwen/Qwen3.8-27B"
updated: "2026-08-18"
---

A colleague sees that Qwen3.8-27B has 27 billion parameters, remembers the famous "twenty tokens per parameter" rule, multiplies, and announces that the model was trained on about 540 billion tokens. It is a natural inference, the arithmetic is right, and the conclusion is almost certainly wrong. Understanding why requires knowing exactly what Chinchilla measured, what it optimized, and what has changed since.

Kaplan-style laws made planning predictable, but their compute-optimal allocation was not the last word. In 2022 Hoffmann and collaborators revisited the question with a broader set of **iso-compute** experiments and produced the model called Chinchilla. The interesting output was not a checkpoint. It was a different answer to "how large should the model be, and how many tokens should it see?"

For a fixed training budget $C$, a dense model faces the approximate constraint $C\propto ND$, with $N$ parameters and $D$ tokens. An iso-compute curve fixes $C$, picks several values of $N$, trains each for the corresponding $D$, and compares validation loss. Too large a model starves for data; too small a model lacks capacity despite abundant tokens. The bottom of that U-shaped curve is the best balance at that budget. Repeat across several budgets, fit how the optimal $N$ and $D$ grow together, and you have a rule. Hoffmann et al. concluded that in their measured setting, parameters and tokens should scale at roughly equal rates. Their headline comparison trained a 70-billion-parameter Chinchilla on 1.4 trillion tokens against the larger 280-billion-parameter Gopher on 300 billion tokens, at the same stated training compute, and Chinchilla won on the reported suite. That ratio, 1.4 trillion over 70 billion, is where "about twenty tokens per parameter" comes from.

Now do the arithmetic your colleague did, but carefully. At twenty tokens per parameter, a 27-billion-parameter dense model would be trained on

$$
D = 20\times 27\times 10^{9}=5.4\times 10^{11}\ \text{tokens},
$$

and the training compute, using the $6ND$ estimate from lesson 5.4, would be

$$
C\approx 6\times(27\times 10^{9})\times(5.4\times 10^{11})=6\times 1.458\times 10^{22}=8.75\times 10^{22}\ \text{FLOPs}.
$$

That is a real number and a useful anchor: it tells you the order of magnitude of a compute-optimal 27B run. It is also entirely our construction. **No token count, compute figure, or training budget is published for Qwen3.8-27B.** There is no technical report; the model card documents architecture, licence, sampling presets, and vendor-reported benchmarks. So treat 8.75e22 FLOPs as "what a Chinchilla-optimal 27B would have cost", never as "what this model cost".

Two refinements before moving on. The $N$ in these fits is non-embedding parameters, and lesson 5.3 showed that roughly 2.54 billion of this model's parameters live in the embedding and untied output head, with more in the vision tower — so a strict application would use something closer to 24 billion and shrink the estimate by around 10%. And $6ND$ was calibrated on uniform attention stacks, whereas 48 of this model's 64 layers are Gated DeltaNet. Both corrections point the same way: these are order-of-magnitude planning figures, and quoting them to three significant digits is false precision.

The conceptual correction Chinchilla delivered was **undertraining**. A parameter earns its keep through many varied token contexts; building more parameters while starving each of data is worse than training a smaller network longer. After 2022, "tokens per parameter" became planning shorthand — but a single ratio cannot capture data quality, architecture, or a shifted loss regime.

:::warning
The most common misconception in this lesson is that twenty tokens per parameter is a law of model training. It is a fitted optimum for one corpus, one tokenizer, one architecture family, one optimizer, and one objective: minimize loss for a fixed **training** budget. Every one of those has moved. Modern open-weight releases routinely train far past it — ratios in the low hundreds of tokens per parameter are ordinary — and they are not making a mistake. They are optimizing something Chinchilla did not measure.
:::

That something is deployment. Chinchilla's question ends the moment training stops. If a model will serve billions of requests, a smaller model trained longer costs more once and less forever. Push our 27B past the compute-optimal point to an illustrative 150 tokens per parameter — again, our assumption, not a published fact — and the budget becomes

$$
D = 150\times 27\times 10^{9}=4.05\times 10^{12}\ \text{tokens},
$$

$$
C\approx 6\times(27\times 10^{9})\times(4.05\times 10^{12})=6.56\times 10^{23}\ \text{FLOPs},
$$

roughly 7.5 times the Chinchilla-optimal budget for the same parameter count. From a training-only viewpoint that is waste. From a serving viewpoint it is a purchase: those extra tokens push a 27-billion-parameter model toward quality that would otherwise have required a much larger one, and the smaller model then reads 54 GB of weights per forward pass instead of several hundred, for every token of every request, for the life of the deployment. Lesson 5.6 turns that trade into an explicit objective, and shows that Qwen3.8-27B's architecture makes the same bet a second time.

:::caveat
The reported counts and fitted coefficients belong to the paper's 2022 corpus, tokenizer, architecture, and methodology. They are evidence about a regime, not a commandment. Reproducing the method also demands more than curve-fitting public checkpoints: recipes must be tuned at every size, token and parameter definitions kept consistent, runs carried to their intended budget without divergence, and the validation set held out and deduplicated per lesson 5.2.
:::

The methodological lesson outlasts the numbers. A famous scaling recommendation changed because experimental coverage and fitting improved, and it will change again. Plan a large run from controlled pilot evidence, attach uncertainty, refit when the pipeline or architecture moves — and remember that compute-optimality answers the question it was asked. "Bigger model" and "more training" compete for the same training budget. Neither of them decides the lifetime economics of a deployed system, which is where this track goes next.
